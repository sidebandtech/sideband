import { createPeer } from "@sideband/peer";

const peer = createPeer({
  endpoint: "ws://localhost:8080",
});

await peer.connect();

// Reconnects, multiplexing, correlation — handled.
const status = await peer.rpc.call("getStatus");
