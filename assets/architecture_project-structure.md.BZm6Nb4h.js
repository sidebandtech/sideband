import{_ as s,o as n,c as a,a6 as o}from"./chunks/framework.BBdhcoHf.js";const u=JSON.parse('{"title":"Project Structure","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/project-structure.md","filePath":"architecture/project-structure.md","lastUpdated":1772729358000}'),t={name:"architecture/project-structure.md"};function r(i,e,p,c,l,d){return n(),a("div",null,[...e[0]||(e[0]=[o(`<div style="display:none;" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /architecture/project-structure.md for this page in Markdown format</div><h1 id="project-structure" tabindex="-1">Project Structure <a class="header-anchor" href="#project-structure" aria-label="Permalink to “Project Structure”">​</a></h1><p>What lives where, who depends on whom, and where to start.</p><h2 id="layout" tabindex="-1">Layout <a class="header-anchor" href="#layout" aria-label="Permalink to “Layout”">​</a></h2><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>.</span></span>
<span class="line"><span>├─ docs/                   # ADRs, specs, architecture notes</span></span>
<span class="line"><span>├─ examples/               # Usage samples (when present)</span></span>
<span class="line"><span>├─ packages/               # Implementation code</span></span>
<span class="line"><span>│  ├─ protocol/</span></span>
<span class="line"><span>│  ├─ transport/</span></span>
<span class="line"><span>│  ├─ runtime/</span></span>
<span class="line"><span>│  ├─ rpc/</span></span>
<span class="line"><span>│  ├─ peer/</span></span>
<span class="line"><span>│  ├─ secure-relay/        # E2EE relay protocol (SBRP)</span></span>
<span class="line"><span>│  ├─ testing/             # Test helpers</span></span>
<span class="line"><span>│  ├─ transport-ws/        # WebSocket transport (browser + Node/Bun)</span></span>
<span class="line"><span>│  ├─ cloud/               # relay.sideband.cloud integration</span></span>
<span class="line"><span>│  └─ cli/                 # Daemon CLI (npx sideband)</span></span>
<span class="line"><span>├─ README.md               # Top-level overview</span></span>
<span class="line"><span>├─ package.json            # Workspace root</span></span>
<span class="line"><span>├─ tsconfig.base.json      # Shared TS options</span></span>
<span class="line"><span>└─ tsconfig.json           # Root project config</span></span></code></pre></div><p>Optional root files (not shown): AI helper notes (<code>CLAUDE.md</code>), lockfiles (<code>bun.lock</code>), lint/format configs.</p><h2 id="package-roles-and-boundaries" tabindex="-1">Package roles and boundaries <a class="header-anchor" href="#package-roles-and-boundaries" aria-label="Permalink to “Package roles and boundaries”">​</a></h2><p><code>@sideband/protocol</code> — Sideband Protocol (SBP) • Canonical message/frame types, constants, error codes; light encode/decode helpers. • No I/O or runtime logic. Topology-agnostic (used by both relay and direct modes). • Depends on: none. Used by: all.</p><p><code>@sideband/transport</code> — Transport ABI + shared utilities<br> • Defines the <code>Transport</code> interface and common transport types/utilities (e.g., reconnect/backoff helpers, optional loopback transport).<br> • No environment-specific code.<br> • Depends on: protocol. Used by: runtime, testing, all concrete transports.</p><p><code>@sideband/runtime</code> — Transport-agnostic runtime • Manages peers, attaches transports, decodes frames, routes messages, correlates requests/responses, middleware hooks. • No concrete transport or UI/framework code. • Depends on: protocol, transport, rpc. Must not depend on concrete transports/cli.</p><p><code>@sideband/rpc</code> — Typed RPC envelope layer • Maps method names to input/output shapes; request/response/notification helpers and JSON codec. • Depends on: protocol. No transport coupling.</p><p><code>@sideband/peer</code> — Peer SDK • Friendly API over runtime + injected or default transport; lifecycle utilities; convenience pub/sub/RPC helpers. • Depends on: protocol, rpc, runtime, transport, transport-* (as chosen). Optional peer dependency: secure-relay. • No protocol definitions or low-level routing.</p><p><code>@sideband/transport-ws</code> — WebSocket transport • Implements the Transport interface via WebSocket for both browser and Node/Bun. • Conditional exports: <code>./browser</code> for browsers, <code>./node</code> for Node/Bun server-side. • Depends on: protocol, transport. Must not depend on runtime/rpc/peer.</p><p><code>@sideband/secure-relay</code> — Sideband Relay Protocol (SBRP) • E2EE handshake, session encryption/decryption, TOFU identity pinning, replay protection. • Enables secure browser↔daemon communication via untrusted relay servers. • Standalone cryptographic layer; no I/O or transport code. • Depends on: none (uses @noble/* for crypto). Used by: relay implementations, browser/daemon apps.</p><p><code>@sideband/cloud</code> — relay.sideband.cloud integration • <code>connect()</code> (client) and <code>listen()</code> (daemon). <code>listen()</code> returns <code>CloudPeerServer</code> with <code>daemonId</code>, <code>relayUrl</code>, and <code>createQuickConnect()</code> for zero-infrastructure Quick Connect URLs. • Depends on: peer, secure-relay, transport-ws.</p><p><code>sideband</code> — Daemon CLI • <code>npx sideband</code> starts a relay-connected daemon and prints a Quick Connect URL. Wraps <code>@sideband/cloud</code>&#39;s <code>listen()</code>. • Depends on: cloud, secure-relay; does not define protocol or runtime behavior.</p><p><code>@sideband/testing</code> — Test scaffolding<br> • Central place for fakes, loopback transports, peer fixtures, and shared test helpers.<br> • Keeps test utilities out of runtime packages; avoids circular devDeps.<br> • Depends on: protocol, transport, and runtime packages as needed. Only imported from tests (never production paths).</p><p>Dependency flow (allowed edges only):</p><div class="language-mermaid"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">graph TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  protocol --&gt; runtime</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  protocol --&gt; rpc</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  rpc --&gt; runtime</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  protocol --&gt; transport</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  transport --&gt; runtime</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  protocol --&gt; transport_ws</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  transport --&gt; transport_ws</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  protocol --&gt; peer</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  transport --&gt; peer</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  rpc --&gt; peer</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  runtime --&gt; peer</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  transport_ws --&gt; peer</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  secure_relay --&gt; peer</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  peer --&gt; cloud</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  secure_relay --&gt; cloud</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  transport_ws --&gt; cloud</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  cloud --&gt; cli</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  secure_relay --&gt; cli</span></span></code></pre></div><h2 id="dependency-policy" tabindex="-1">Dependency policy <a class="header-anchor" href="#dependency-policy" aria-label="Permalink to “Dependency policy”">​</a></h2><ul><li><strong><code>dependencies</code></strong> — anything your published JS imports at runtime. All internal <code>@sideband/*</code> imports go here. Each package must be installable in isolation without consumers manually wiring internal pieces.</li><li><strong><code>peerDependencies</code></strong> — only for host-provided integrations or optional feature adapters the consumer chooses to install (e.g., <code>@sideband/secure-relay</code> in peer).</li><li>Never use peer dependencies just to deduplicate installs across the monorepo; package managers already deduplicate compatible <code>^</code> ranges.</li></ul><h2 id="conventions" tabindex="-1">Conventions <a class="header-anchor" href="#conventions" aria-label="Permalink to “Conventions”">​</a></h2><ul><li>Language: TypeScript; ESM (<code>type: &quot;module&quot;</code>).</li><li>Each package has its own <code>package.json</code> and <code>tsconfig.json</code> extending <code>tsconfig.base.json</code>.</li><li>Tests/config/docs live with the package they describe.</li><li>Keep new code within the package that owns the responsibility above; update this file when roles or dependencies change.</li></ul>`,23)])])}const g=s(t,[["render",r]]);export{u as __pageData,g as default};
